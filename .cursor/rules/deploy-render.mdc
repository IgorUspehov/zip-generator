---
description: Deploy ONLY the isolated zip-generator Render service — never saas-mvp-funnel / main / webstudio-muenchen.com
alwaysApply: true
---

# Deploy — zip-generator ONLY

## Hard rule

- Work and deploy **only** Render service **`zip-generator`** (branch `zip-generator`).
- **Forbidden forever:** push/change `main`, touch service **`saas-mvp-funnel`** (`srv-da1dg061egvs73ai5ubg`), or change **https://webstudio-muenchen.com**.
- **Forbidden:** Railway / Netlify / Vercel as production for this workstream.

## This service

- **Name:** `zip-generator`
- **Git branch:** `zip-generator` only
- **Blueprint:** `render.yaml` on branch `zip-generator` (service name must stay `zip-generator`)
- **URL:** the `*.onrender.com` URL of **zip-generator**, never webstudio-muenchen.com

## When user says «деплой» / «пуш»

1. Commit on branch **`zip-generator`** (no secrets; do not delete `data/manifests/*`).
2. `git push origin zip-generator` — **never** `main`.
3. Confirm the **zip-generator** service URL (not webstudio-muenchen.com).

## Env / secrets

Configure env only on the **zip-generator** Render service. Do not edit env on saas-mvp-funnel.
